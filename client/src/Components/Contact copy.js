import React from 'react'

const Contact copy = () => {
    return (
        <div>
            <div>
      <h1>{counter}</h1>
      <Button onClick={() => setCounter()} color="secondary">
        Add One
      </Button>
      <button>Better</button>
    </div>
        </div>
    )
}

export default Contact copy
